# =============================================================================
# Firebase Configuration (required for authentication and data persistence)
# =============================================================================

# Firebase project ID (required)
FIREBASE_PROJECT_ID=gitscout-demo

# Firebase credentials (choose ONE method):
# Method 1: Path to service account JSON file (for local development)
FIREBASE_CREDENTIALS_PATH=./serviceAccountKey.json

# Method 2: Service account JSON as string (for Cloud Run via Secret Manager)
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

# Method 3: Application Default Credentials (automatic on Cloud Run/GCE)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# For production, add your deployed frontend URL:
# CORS_ORIGINS=http://localhost:5173,https://your-app.vercel.app

# =============================================================================
# GitHub API Token (required)
# =============================================================================
# Get your token from: https://github.com/settings/tokens
# Required scopes: public_repo, read:user
GITHUB_TOKEN=your_github_token_here

# Default LLM Provider (optional)
# Options: gemini, groq, ollama, mock
# Default: gemini
DEFAULT_LLM_PROVIDER=gemini

# LLM API Keys (optional - only needed if using those providers)

# Gemini API Key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_key_here

# Groq API Key
# Get from: https://console.groq.com/keys
GROQ_API_KEY=your_groq_key_here

# Ollama Base URL (optional - for local LLM)
# Default: http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434

# =============================================================================
# GitScout API Limit Configuration (optional)
# Adjust these values to control GitHub API usage and avoid rate limits
# =============================================================================

# Maximum repos to process in the pipeline
# Default: 10
GITSCOUT_MAX_REPOS=10

# Number of contributors to fetch per repository
# Default: 10
GITSCOUT_CONTRIBUTORS_PER_REPO=10

# Number of repos fetched per search query
# Default: 50
GITSCOUT_REPOS_PER_QUERY=50

# Number of top contributors to hydrate with full profile data
# Default: 50
GITSCOUT_TOP_CONTRIBUTORS=50

# Batch size for user hydration GraphQL requests
# Lower values = more requests but less likely to hit limits
# Default: 5
GITSCOUT_HYDRATE_BATCH_SIZE=5

# Maximum concurrent REST API requests
# Default: 5
GITSCOUT_MAX_CONCURRENT_REQUESTS=5

# =============================================================================
# Chat Agent Configuration (optional)
# =============================================================================

# Maximum tokens allowed per chat conversation
# Default: 10000
GITSCOUT_CHAT_MAX_TOKENS=10000

# Maximum clarifying questions the AI can ask per conversation
# Default: 3
GITSCOUT_CHAT_MAX_CLARIFICATIONS=3
